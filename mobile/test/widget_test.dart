import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App compiles', (WidgetTester tester) async {
    // Placeholder — real tests require GraphQL mock setup
    expect(true, isTrue);
  });
}
